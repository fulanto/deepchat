# LDAP Login Button Click Plan

1. Fix LDAP page drag/no-drag CSS and use the project's `window-no-drag-region` naming.
2. Add an in-flight state to avoid duplicate clicks and make clicks observable.
3. Add a renderer component test for button click behavior.
4. Archive the issue SDD folder after implementation.
